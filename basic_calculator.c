#include<stdio.h>
void main()
{
int sum,sub, result;
printf("Enter a number: %d");

switch(result);
{
case1:
    printf("add the two number:");

    break;
    sum();
case2:
    printf("substract the two number:");
    sub();
    break;
}
}
int sum()
{
int a,b;
printf("Enter two number:");
scanf("%d %d", &a,&b);
result(a+b);
}
int sub()
{
    int x,y;
    printf("Type two number:");
    scanf("%d %d", &x,&y);
    result(x-y);
}


